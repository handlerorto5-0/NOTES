Функции:
a_descriptive_name(optional_arguments):
""" Строка документации"""
# однострочный коментарий
	any code...
	return optional_value
PEP 8 — это руководство по стилю оформления кода на языке Python.
http://pep8.ru/doc/pep8/
PEP 257,посвящен форматированию строк документации

bool() функция, которая по переданному параметру может определить, истинный он или ложный. Объект считается ложным, если он равен 0, значению None, пустой строке или пустой встроенной структуре данных, любая непустая структура данных — это True.
return пустое множество выглядит как set()

Аннотации документируют — стандартным способом — тип возвращаемого значения, а также типы всех аргументов
def search4vowels(word:str) -> set:
интерпретатор Python не использует аннотации для проверки типов аргументов

Значение по умолчанию:
letters:str='aeiou'

Позиционные и именованные аргументы:
вызов по ключу- позиционное присваивание
search_letters(letters='xyz', phrase='galaxy')

--------------------------------------------------------
Модули:
